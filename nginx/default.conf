server {
    listen 80;
    server_name _;           # ← replace with your domain: e.g. fintrack.yourdomain.com

    # ── Serve frontend static files ──────────────────────
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # ── Proxy API requests to FastAPI ────────────────────
    location /api/ {
        proxy_pass         http://api:8000/;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    # ── For Let's Encrypt SSL certificate ────────────────
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
}

# Uncomment after getting SSL cert:
# server {
#     listen 443 ssl;
#     server_name fintrack.yourdomain.com;
#     ssl_certificate     /etc/letsencrypt/live/fintrack.yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/fintrack.yourdomain.com/privkey.pem;
#     ... (same location blocks as above)
# }
